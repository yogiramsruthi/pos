{
  "name": "@pos/desktop",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "build": "echo 'Desktop packaging to be configured with electron-builder'",
    "lint": "echo 'No linter configured for desktop'",
    "test": "node --test"
  },
  "dependencies": {
    "electron": "^39.8.1"
  }
}
